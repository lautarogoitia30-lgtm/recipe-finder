[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn recipe_finder.wsgi --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/recipes/health/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
